spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  application:
    name: NotificationService
  config:
    import: optional:configserver:http://localhost:8888
